```
Lightweight API testing tool

Usage: catalyst <PATH>

Arguments:
  <PATH>
          Path to the test file or directory

          If a directory is provided, all files with the .nix extension will be tested.

Options:
  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
```
